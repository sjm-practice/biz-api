{
  "name": "biz-api",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "lint": "eslint .",
    "pretest": "npm run lint --silent",
    "mocha-unit": "mocha ./tests/unit --compilers js:babel-register",
    "chimp": "chimp --ddp=http://localhost:3000 --mocha --chai --path=./tests/end-to-end/mocha --browser=phantomjs"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-register": "^6.9.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "testdouble": "^1.4.2"
  }
}
